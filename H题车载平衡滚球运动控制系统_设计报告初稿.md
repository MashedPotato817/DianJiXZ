# 车载平衡滚球运动控制系统（H 题）设计报告初稿

> 版本：基于当前工作区源码整理  
> 工程版本：`feat/k230-detect2`（提交 `423b1c0`，另含尚未提交的在研修改）  
> 主控工程：`project/mspm0/`  
> 视觉工程：`project/k230/`  
> 说明：本文用于撰写/完善竞赛设计报告。文中“源码已实现”仅说明相应逻辑已存在于当前代码；除非在“测试结果”中填入可追溯原始记录，否则不得表述为实车指标已达成。

## 摘要

本系统面向 2026 年全国大学生电子设计竞赛江苏赛区赛（TI 杯）H 题“车载平衡滚球运动控制系统”。系统以 MSPM0G3507 为底盘与摆杆控制核心，保留差速小车的编码器速度闭环、八路红外灰度循线、OLED 显示和启动按键；以 K230 相机完成摆杆凹槽内钢球的视觉检测，并经 UART 向主控发送钢球位置、有效标志、序号和边缘丢失方向。主控根据视觉位置计算舵机摆杆角度，通过分阶段的起球、运行、制动、过零阻尼、目标保持与失联保护策略，使钢球向给定位置收敛；同时利用自动扫掠标定获取当前机械安装条件下的摆杆平衡角，并可将标定结果写入片内 Flash。

针对车载环境中存在的底盘加减速扰动、视觉帧率低于控制周期、钢球静摩擦与管槽局部凹陷、通信失帧和小球出视野等问题，软件采用“5 ms 底盘/执行器控制节拍 + 视觉异步采样 + 50 ms 遥测记录”的分层结构。K230 至 MSPM0 的数据帧采用 CRC-8/ATM 校验，主控统计解析错误、CRC 错误、序号间断和接收溢出，并通过 UART0 输出带时间戳的遥测记录，便于对位置、速度估计、舵机输出和控制状态进行联合分析。

当前工程已具备完整的软件结构和现场联调接口，但视觉标定、整车循线、静止点位、行驶稳球及图传录像等最终性能仍需按赛题条件实测。本报告给出可直接扩展为最终设计报告的方案论证、理论分析、软硬件设计、测试方案、结果表格和风险分析。

**关键词：** MSPM0G3507；K230；机器视觉；滚球平衡；舵机控制；红外循线；串口通信

## 1. 任务理解与设计目标

### 1.1 赛题任务

H 题要求制作一辆载有带凹槽摆杆的循线小车。小车沿黑色闭环路线行驶时，摆杆中的钢球应稳定在中心点或指定位置附近。摆杆左端由铰链固定，右端由角度控制机构驱动；摄像头需覆盖摆杆全程，完成钢球运动画面的实时显示、录像和位置判定。

赛题原文及评分表见 [project/topic/车载平衡滚球运动控制系统_H题_markdown版_图片预览版.md](project/topic/车载平衡滚球运动控制系统_H题_markdown版_图片预览版.md)。本文将核心要求归纳如下。

| 项目 | 赛题要求 | 本工程的软件对应项 | 当前证据状态 |
| --- | --- | --- | --- |
| 图传与录像 | 车载发送、场外接收，实时显示且完整录像 | K230 检测预览与 UART 脚本已存在；录像/接收端流程需实机确认 | 待实测 |
| 一圈循线 | A 点启动，顺时针一圈，≤20 s，停车偏差≤2 cm | 八路灰度循线、差速速度 PI、OLED/按键底座已保留 | 待按赛道实测 |
| 静止定点 | 0 → +5 cm → −5 cm，≤5 s，最大误差绝对值≤1 cm | `ball_task` 状态机、K230 位置反馈、舵机控制 | 源码已实现，指标待实测 |
| A→B 稳球 | AB ≤8 s，钢球中心误差绝对值≤1 cm | 底盘循线与滚球闭环并行执行 | 待实测 |
| 全圈中心稳球 | 一圈≤30 s，中心误差绝对值≤1 cm | 同上，含通信超时/边缘恢复策略 | 待实测 |
| 全圈任意点稳球 | 一圈≤30 s，指定位置误差绝对值≤1 cm | 控制器具有目标位置接口，任务状态机当前固定演示 ±50 mm | 任意点上位接口与实测待补 |

### 1.2 约束条件

- 循迹传感器只能采用红外光电模块；工程沿用八路红外灰度传感器。
- 小车应有启动按键与显示装置；当前使用 PA18 按键与 OLED 显示。
- 钢球位置检测必须采用摄像头；当前采用 K230 相机方案。
- 摆杆为约 25 cm 长的 PPR 管改造凹槽，钢球直径约 1 cm。槽内不允许靠粗糙化或凹坑增摩擦实现稳定。
- 设计报告评分覆盖方案论证、循线与摆杆控制理论、电路和程序、测试结果以及报告规范性。因此软件设计与原始测试数据必须一一对应。

### 1.3 本系统的可验证目标

为避免“代码存在”等同于“指标完成”，将工作分为以下三个层次：

1. **软件功能正确性**：工程能编译；接口、状态机、协议和安全分支按设计执行。
2. **台架闭环正确性**：在静止车体上验证视觉位置方向、舵机方向、平衡基准、定点控制和断链保护。
3. **赛道性能正确性**：在规定路线、指定负载、电池状态和计时规则下验证循线、停车、AB 段、整圈及任意点稳球指标。

最终报告中必须分别给出三类证据，不应以第 1 类证据代替第 2、3 类结果。

## 2. 系统总体方案

### 2.1 总体结构

```text
                         ┌────────────────────────┐
                         │  K230 相机与视觉程序    │
                         │ 图像采集/球心检测/标定  │
                         └───────────┬────────────┘
                                     │ UART1, 115200 bit/s
                                     │ $K230,BALL,...*CRC#
┌─────────────┐  灰度/编码器  ┌──────▼───────────────────────┐  PWM  ┌───────────┐
│ 黑色赛道    ├──────────────►│ MSPM0G3507                   ├──────►│ 摆杆舵机  │
└─────────────┘               │ ├─巡线与差速速度闭环         │       └─────┬─────┘
                              │ ├─K230 接收、校验与诊断      │             │ 摆角
┌─────────────┐               │ ├─滚球状态机与位置控制       │       ┌─────▼─────┐
│ 启动按键    ├──────────────►│ ├─自动平衡角标定与 Flash     │       │ 凹槽摆杆  │
└─────────────┘               │ └─OLED / UART0 遥测          │       │ + 钢球    │
                              └──────┬───────────────────────┘       └───────────┘
                                     │ PWM/GPIO
                              ┌──────▼───────┐
                              │ TB6612 + 电机 │
                              │ 差速底盘      │
                              └──────────────┘
```

系统将“底盘沿线运动”和“摆杆滚球调节”视为并行子系统：前者决定车体加速度与振动扰动，后者通过摄像头位置反馈抑制钢球相对摆杆的位移。二者共享 MSPM0 的 5 ms 定时中断，但控制变量不同：底盘输出左右轮 PWM，滚球输出舵机脉宽。

### 2.2 分层软件架构

| 层次 | 主要文件 | 职责 |
| --- | --- | --- |
| 板级与外设 | `Hardware/motor.*`、`encoder.*`、`servo.*`、`k230_link.*`、`calib_store.*` | 电机、编码器、舵机 PWM、UART 接收、Flash 持久化 |
| 底盘控制 | `Control/control.*` | 灰度采样、曲率转向、轮速换算、左右轮增量 PI |
| 滚球控制 | `Control/ball_control.*` | 位置/速度估计、舵机输出、状态切换、丢球与边缘保护 |
| 任务与标定 | `Control/ball_task.*`、`ball_calibrate.*` | ±50 mm 定点任务、按键触发、平衡角扫掠标定 |
| 观测与交互 | `Control/show.c`、`debug_telemetry.*` | OLED 现场显示、UART0 CSV 遥测与事件记录 |
| 视觉端 | `k230_ball_detect_uart.py`、`ball_detect_config.py` | 图像检测、像素到毫米映射、UART 发送、日志 |

### 2.3 实际工程目录

```text
project/
├── topic/                  # 赛题原文、软件工作流、结构示意图
├── docs/                   # 接线、巡线和调试文档
├── mspm0/                  # MSPM0G3507 Keil 工程与 SysConfig
│   ├── Control/            # 底盘、滚球、任务、标定、遥测
│   ├── Hardware/           # 驱动和通信模块
│   ├── keil/               # uVision 工程
│   └── empty.syscfg        # 外设和引脚配置源
└── k230/                   # CanMV 视觉脚本、配置、协议说明
```

工程以 `firmware/WHEELTEC_C07A_CAR` 的已知 MSPM0/Keil/SysConfig 底座为基础；模块划分借鉴 `firmware/Medicine_Cart` 的工程化方式。对于自动生成的 `ti_msp_dl_config.c/h`，只允许通过 `empty.syscfg` 和 TI SysConfig 重新生成，不能手工修改。

## 3. 硬件与接口设计

### 3.1 主控与执行部件

主控采用 MSPM0G3507（Cortex-M0+）。电机驱动采用 TB6612 双 H 桥，左右电机使用编码器闭环；摆杆由单路舵机驱动。当前 SysConfig 中，电机 PWM 定时器为 TIMA1，计数周期为 8000；舵机 PWM 为 TIMA0 CCP0，计数周期为 20000。舵机软件将该输出按 1 MHz 计数解释为 20 ms 周期、微秒级脉宽。

| 功能 | 当前工程配置 | 代码/配置依据 | 使用注意 |
| --- | --- | --- | --- |
| 左右电机 PWM | TIMA1，PB2/PB3，周期 8000 | `project/mspm0/empty.syscfg` | 电机方向脚与实际正方向应在整车上复核 |
| 舵机 PWM | TIMA0 CCP0，PA8，周期 20 000 | `empty.syscfg`、`Hardware/servo.h` | PA8 必须接舵机信号线；舵机电源与主控共地 |
| K230 串口 | MSPM0 UART1：PB7 为 RX、PB6 为 TX，115200 bit/s | `empty.syscfg`、`Hardware/k230_link.h` | K230 接线保持现状，禁止在无硬件确认时改 SysConfig |
| 遥测串口 | MSPM0 UART0：PA11 为 RX、PA10 为 TX，115200 bit/s | `empty.syscfg`、`debug_telemetry.c` | 与 K230 通信分离，用于电脑记录 |
| 启动按键 | PA18 | `empty.syscfg` | 短按/长按行为应在现场文档中明确 |
| OLED | PB14、PB15、PA28、PA31 | `empty.syscfg` | 软件刷新已节流到 200 ms |
| 灰度传感器 | 地址 PA27、PA12、PB16；数据 PB8 | `empty.syscfg`、`control.c` | 黑线电平由 `GRAY_BLACK_LEVEL` 定义，应复核极性 |
| 左/右编码器 | PA26/PA25，PB24/PB20 | `empty.syscfg` | 工程按两倍频和 13 线、28:1 参数换算 |

### 3.2 K230 UART 接线与帧格式

当前工程对 K230 的接线约定如下，接线不作为本轮改动对象：

```text
K230 IO40 / UART1_TX  ───►  MSPM0 PB7 / UART1_RX
K230 IO41 / UART1_RX  ◄───  MSPM0 PB6 / UART1_TX
K230 GND              ────  MSPM0 GND
通信参数：115200 bit/s，8N1
```

K230 发送的安全帧为：

```text
$K230,BALL,<x10>,<valid>,<seq>,<edge>*<crc8>#\r\n
```

其中 `x10` 为钢球相对摆杆中心位置的毫米值乘以 10；`valid` 为视觉有效标志；`seq` 为递增帧序号；`edge` 为边缘丢失方向（−1 左侧、+1 右侧、0 为正常或普通漏检）。`crc8` 使用 CRC-8/ATM，生成多项式为 `0x07`，初值为 `0x00`，覆盖 `$` 之后至 `*` 之前的 ASCII 数据。

MSPM0 仅允许位置位于 ±150 mm、字段完整且 CRC 正确的帧进入闭环。接收端使用 128 字节环形缓冲、64 字节行缓冲，并记录 `rx_overruns`、`rx_hw_overruns`、`parse_errors`、`crc_errors`、`range_errors` 和 `sequence_gaps`。合法首帧会回送一次 `$MSPM0,ACK#`；此外按 500 ms 周期发送 HELLO，作为链路联调提示，而非逐帧确认机制。

### 3.3 舵机角度与脉宽映射

当前舵机抽象层采用绝对角度表示：

| 参数 | 当前值 | 说明 |
| --- | ---: | --- |
| 可用角度范围 | 5°～175° | 软件限幅，避免机构碰撞 |
| 脉宽范围 | 1100～2100 μs | 当前机构的角度—脉宽换算范围 |
| 平衡初始角 | 100.8° | 当前固定机械基准 |
| 平衡初始脉宽 | 1660 μs | 与 100.8° 对应 |
| 换算关系 | `(2100−1100)/180 ≈ 5.56 μs/°` | `Servo_SetTargetAngle()` 内部使用 |

定义摆杆中心平衡角为 `θ_trim`，控制器计算相对偏置 `u` 后，舵机命令为：

\[
\theta_{cmd}=\mathrm{sat}(\theta_{trim}+u,\;5^\circ,\;175^\circ)
\]

当前机械约定为“角度增大、PWM 脉宽增大时，摆杆右端降低”。该方向必须在上电、低幅度、无球或防坠球条件下再次确认；若方向与视觉坐标定义相反，不能只改比例参数，应先统一坐标与机械正方向。

## 4. 小车循线与底盘控制理论

### 4.1 灰度位置估计

八路灰度传感器由三根地址线轮询选通。第 `i` 个传感器的横向坐标按照等间距布置为：

\[
x_i\in\{-3.5d,-2.5d,\ldots,3.5d\}
\]

其中当前传感器总跨距 `85 mm`，相邻间距 `d=85/7 mm`。对被判为黑线的通道取均值，获得横向偏差：

\[
x_{line}=\frac{\sum b_i x_i}{\sum b_i}
\]

其中 `b_i` 为黑线识别结果。当前 `GRAY_BLACK_LEVEL=1`，需以实物白底/黑线电平验证。若未检测到黑线，系统不继续前进；若此前存在有效偏线方向，则以最后有效方向低速原地搜线，最大累计搜线角为一圈。

### 4.2 曲率转向与差速运动学

将灰度传感器位置换算为 `y`，传感器相对轮轴中点前向距离为 `L`，使用几何前视曲率：

\[
\kappa=\frac{2y}{L^2+y^2}
\]

车辆线速度为 `v` 时，角速度命令为：

\[
\omega=-K_s v\kappa
\]

源码中当前低速联调参数为：基础速度 `45 mm/s`、前视距离 `134 mm`、转向增益 `1.70`、角速度限幅 `0.40`。这些值不是赛题最终速度参数；应在完成视觉稳球和底盘稳定性验证后，按“直线—弯道—全圈”的顺序递增。

设轮距为 `B=0.1780 m`，则左右轮目标线速度为：

\[
v_L=v-\frac{B}{2}\omega,\qquad v_R=v+\frac{B}{2}\omega
\]

编码器速度换算采用轮径 `0.0768 m`、轮周长 `πD`、13 线、两倍频、28:1 减速比，因此每轮一圈对应计数为：

\[
CPR=13\times 2\times 28=728
\]

### 4.3 电机增量 PI 速度闭环

左右轮分别采用增量式 PI。令速度误差为 `e(k)=v_{ref}(k)-v(k)`，PWM 输出满足：

\[
u(k)=u(k-1)+K_p[e(k)-e(k-1)]+K_i e(k)
\]

当前代码初值为 `Kp=400`、`Ki=300`，控制频率为 200 Hz，PWM 限幅为 ±7800，并设置 `0.005 m/s` 死区和一阶速度滤波（系数 `0.4`）。增量 PI 的优点是便于直接对 PWM 增量施加限幅；但参数必须在车轮离地、直线地面、赛道弯道和携带摆杆负载等不同条件下检查，避免积分残留导致起步冲击。

### 4.4 底盘与滚球耦合分析

小车在弯道、启停和路面不平处产生的纵向/横向加速度，会改变钢球在摆杆坐标系下的等效受力。仅采用静态角度补偿不能保证行驶稳球，故系统采用视觉闭环直接观测相对位置。底盘控制方面应避免骤变：

- 启动和制动使用平滑的速度规划，减少摆杆上的等效惯性冲击；
- 弯道速度应低于直线速度，并以日志比较弯道处最大位置误差；
- 若钢球控制器报告失联、边缘恢复超时或任务故障，比赛模式应进入明确的安全策略（例如停车并回中），该整车联锁尚需根据赛场安全要求落实与验证。

## 5. K230 视觉测量设计

### 5.1 检测流程

视觉脚本 `project/k230/k230_ball_detect_uart.py` 与配置文件 `ball_detect_config.py` 构成 K230 端。当前默认方案为灰度图圆检测：在 320×240 图像中限制摆杆可见的纵向区域，执行 `cv_lite.grayscale_find_circles()`，再对候选球心进行连续帧确认、跳变抑制和丢失判定。

主要配置如下：

| 配置项 | 当前值 | 作用 |
| --- | ---: | --- |
| 图像尺寸 | 320×240 | 相机处理分辨率 |
| 纵向 ROI | `(0, 50, 320, 80)` | 限制在摆杆运动带，降低误检 |
| 最小连续确认帧 | 3 | 连续检测后才输出 `valid=1` |
| 连续丢失帧 | 3 | 达到后输出 `valid=0` |
| 最大球心跳变 | 28 px | 过滤不连续候选点 |
| 发送周期 | 50 ms | 配置目标为 20 Hz，需以日志实测 |
| 坐标范围 | ±150 mm | 发送及主控接收安全边界 |

若圆检测在实际光照、钢球反光条件下不稳定，代码保留了 `blob` 检测模式作为替代，但不能只凭电脑端 Python 环境判断 CanMV API 可用性；必须在目标 K230 固件中确认 `cv_lite` 导入、帧率与检测稳定性。

### 5.2 像素到毫米的标定

视觉横坐标以图像右侧为正。当前配置使用三点、左右分段线性映射：

| 实体位置 | 当前像素锚点 |
| --- | ---: |
| −50 mm | 64 px |
| 0 mm | 162 px |
| +50 mm | 256 px |

左、右比例分别为：

\[
k_L=\frac{50}{162-64}\ \mathrm{mm/px},\qquad
k_R=\frac{50}{256-162}\ \mathrm{mm/px}
\]

因此对于像素横坐标 `p`：

\[
x_{mm}=\begin{cases}
(p-162)k_L,&p<162\\
(p-162)k_R,&p\ge 162
\end{cases}
\]

当前配置明确标记 `CALIBRATION_READY=False`：三点数据仅用于预览和低幅度联调，不能用于精度结论或正式闭环验收。最终应至少在 −100、−50、0、+50、+100 mm 等位置各重复 3 次，记录均值、标准差、最大绝对误差，并确认全行程的符号与线性误差。

### 5.3 图传与日志

视觉端每次启动生成会话编号，在 K230 侧保存 `<session>_K230.txt`；电脑串口助手使用同一会话编号保存 MSPM0 的 `<session>_M0.txt`。日志中应包含视觉位置、实际发送值、帧序号、帧率、链路 ACK 和状态变化。赛题要求的实时画面和完整视频，还应补充：接收端设备型号、录像软件、视频文件命名规则、视频时钟与遥测会话编号关系，以及每次测试对应的视频路径。

## 6. 摆杆滚球控制理论与实现

### 6.1 控制对象简化模型

将钢球视为沿摆杆凹槽一维运动的对象。摆杆小角度倾斜 `α` 时，沿槽方向重力分量可近似写为：

\[
F_g\approx mg\sin\alpha\approx mg\alpha
\]

钢球还受滚动阻力、静摩擦、槽面局部不平、车体加速度和舵机动态影响。其精确模型与钢球转动惯量、接触条件有关；工程实现不依赖精确质量参数，而使用视觉位置 `x` 与离散速度 `\dot{x}` 的反馈，并通过状态机处理“静摩擦脱困—接近目标—过零阻尼—目标保持”的非线性过程。

令目标位置为 `x_r`，误差为：

\[
e=x_r-x
\]

在常规运行阶段，控制输出以 PD 与条件积分保持项构成：

\[
u=K_p e-K_d\dot{x}+u_I
\]

其中当前代码初始参数为 `Kp=0.24 °/mm`、`Kd=0.040 °·s/mm`。速度不是以 5 ms 控制周期差分，而是仅在收到新的有效 K230 样本时更新，并进行一阶滤波。这避免了把同一视觉样本重复差分而放大噪声。

### 6.2 条件积分与目标保持

PD 控制在误差接近零时输出趋近于零，若摆杆存在微小静态斜率，钢球可能慢慢偏离目标。因此程序仅在“接近目标且低速”时积分：

\[
u_I(k)=\mathrm{sat}\bigl(u_I(k-1)+K_i e(k)T_s,\;-6^\circ,\;6^\circ\bigr)
\]

当前积分带宽为 `|e|≤12 mm`，速度条件为 `|v|≤12 mm/s`，积分系数为 `0.30 °/(mm·s)`。离开目标区域时清除积分；高速运行时暂停累积。该设计的目的不是替代机械调平，而是在已调平基础上抵消小范围静态偏置。

### 6.3 分阶段状态机

控制器状态定义为 OFF、CAPTURE、ACCEL、RUN、BRAKE、EDGE、FAULT、HOLD、LOST 和 PASS。核心逻辑如下。

```text
上电/复位
   │
   ├─ 无有效视觉或超时 ─────────────────────────► LOST / 安全回中
   │
   └─ 有效视觉 ─► CAPTURE ─► ACCEL ─► RUN ─► BRAKE ─► PASS ─► RUN
                                  │       │                    │
                                  │       └─ 目标附近低速 ───► CAPTURE/保持
                                  │
                                  ├─ 位置在边缘后丢失 ───────► EDGE
                                  └─ 加速超时 ───────────────► FAULT
```

1. **CAPTURE**：当钢球在目标附近且低速时，以受限阻尼和条件积分使其稳定在目标点。
2. **ACCEL**：若钢球远离目标且速度不足，采用分级倾角克服静摩擦。当前首级为 24°，每 300 ms 增加 4°，最高 36°，最长 2000 ms。对于 2～20 mm 的较小初始误差，整条起球倾角按 0.5～1.0 线性缩放，降低近中心处的过冲风险。
3. **RUN**：常规 PD 阶段，不设置固定同向最小倾角，避免持续输入动能。
4. **BRAKE**：基于当前朝向目标的速度及固定减速度 `250 mm/s²` 估计停车距离，在到达目标前提前制动。
5. **PASS**：首次过零后至少保持 150 ms 的有限阻尼，避免立即反向进入大角度 ACCEL 而形成反复过零。反侧目标距离取起始误差的 20%，并限制为 3～8 mm。
6. **HOLD/LOST**：K230 链路判定 100 ms 超时后，控制器额外保持上一安全输出至总计 180 ms；仍未恢复则进入 LOST，清除速度样本并回到受限安全输出。
7. **EDGE**：若最后可靠位置靠近视野边缘后丢失，按 `edge_direction` 施加相对平衡角 50° 的恢复倾角；超过 1000 ms 仍未恢复时停止恢复动作。
8. **FAULT**：起球阶段超时或任务阶段超时后进入故障状态，避免无限制持续驱动。

### 6.4 静止 ±50 mm 演示任务

`ball_task.c` 实现赛题静止定点测试的任务骨架：按键启动后先设目标为 `+50 mm`，稳定后切换至 `−50 mm`，完成后输出总耗时和两个阶段的最大误差。

任务验收使用以下软件条件：

| 条件 | 当前软件阈值 | 与赛题关系 |
| --- | ---: | --- |
| 目标点误差 | ≤10 mm | 与赛题 ±1 cm 对齐 |
| 速度绝对值 | ≤10 mm/s | 避免仅瞬间穿越目标即判完成 |
| 连续稳定时间 | ≥500 ms | 证明不是瞬时到达 |
| 最少有效稳定样本 | 4 个 | 配合异步视觉样本 |
| 每阶段超时 | 8000 ms | 故障退出保护，不等同于赛题 5 s 指标 |

任务软件阈值是“判定条件”，而赛题要求的是通过完整测试得到的最大误差和总时间。因此最终报告应以日志和视频重新计算，不可直接把这些宏定义当作测试结果。

### 6.5 自动平衡角标定与持久化

机械安装、舵机花键重新装配或摆杆形变都会改变水平角。当前自动标定由按键长按触发，流程为：

```text
IDLE → WAIT_BALL → SETTLE → OBSERVE → 更新搜索区间 → … → DONE
```

其方法是在 50°～150° 的搜索区间内试探舵机角度，等待 400 ms 后观察 500 ms 的 K230 位置趋势；由钢球向哪一侧滚动判断当前倾斜方向，逐步缩小区间。收敛条件为搜索区间不超过 1°或最多 8 次迭代。等待有效钢球最长 3 s，全流程最长 20 s。标定激活期间冻结正常滚球控制，避免两个模块同时写舵机。

标定完成后由主循环（非 5 ms 中断）执行 Flash 擦写与回读校验，保存到片内主 Flash 的保留扇区。保存采用数据先写、提交标志后写的顺序，并在擦写期间关闭中断，降低在写入区域取指的风险。Flash 操作属于低频事件；是否影响 K230 数据连续性、掉电后是否成功恢复，必须纳入实测记录。

## 7. 程序流程与实时性设计

### 7.1 上电初始化流程

`project/mspm0/empty.c` 的主流程依次完成：

1. 执行 `SYSCFG_DL_init()` 初始化 SysConfig 配置的外设；
2. 清除并使能编码器、5 ms 定时器和电压 ADC 中断；
3. 将 UART1 中断优先级设为 0、定时器设为 1，使串口接收可抢占灰度采样中的忙等待；
4. 初始化 OLED、K230 链路、舵机、滚球控制器、自动标定和任务状态机；
5. 若 Flash 内存在有效标定值，则加载为当前平衡角；否则使用 100.8° 默认基准；
6. 初始化 UART0 遥测并输出 `RESET`、`TRIM`、`TARGET` 等事件；
7. 在主循环中处理 UART 解帧、遥测发送、运行时参数命令、标定保存、ADC 电压和 OLED 刷新。

### 7.2 5 ms 定时中断流程

```text
TIMER_0 中断（200 Hz）
 ├─ K230_Link_Tick5ms()：链路时间基准
 ├─ Ball_Calibrate_Tick5ms()：非阻塞标定状态推进
 ├─ 记录标定/任务状态变化事件
 ├─ 获取最新 K230 位置
 ├─ 若非标定状态：Ball_Control_Step(position, 0.005 s)
 ├─ Ball_Task_Tick5ms()：定点任务判定
 ├─ 按键处理与 LED 链路指示
 ├─ Debug_Telemetry_Tick5ms()
 ├─ 编码器测速、灰度循线或遥控目标更新
 └─ 左右轮增量 PI 与 TB6612 PWM 输出
```

视觉端配置发送周期为 50 ms，明显慢于 5 ms 主控周期。因此必须把“控制时基 200 Hz”与“新的视觉测量约 20 Hz（实际需测量）”区分开：控制器每 5 ms 执行安全与状态逻辑，但只有新样本到达时才更新视觉速度。报告中应同时给出 K230 实际帧率、UART 发送周期分布、MSPM0 接收时延分布和控制输出刷新周期。

### 7.3 中断优先级与串口可靠性

灰度采样中每通道存在约 50 μs 延时，八通道总忙等待不少于 400 μs。115200 bit/s 下 UART 接收 FIFO 容量有限，若 UART 与定时器同优先级，定时器执行期间到达的字符可能在硬件 FIFO 覆盖而未进入软件环形缓冲。工程将 UART1 设置为更高优先级，以便其抢占定时器中断，降低这种隐蔽丢字节风险。

该措施仍不能替代实际压力测试。建议在报告测试部分给出高速连续发送、灰度循环启用、舵机动作启用三种情况下的 `rx_hw_overruns`、CRC 错误和序号间断计数。

## 8. 人机交互、显示与遥测设计

### 8.1 OLED 显示

OLED 每 200 ms 刷新一次，以免软件模拟 SPI 刷新占用主循环。当前页面显示：

- 舵机 PWM 脉宽（μs）；
- 最近一次有效钢球位置 `X`（mm）；无有效视觉时显示 `--.- mm`；
- 当前任务模式：标定 `CAL`、`+5`、`−5` 或 `ZERO`。

最终比赛显示还应补充或明确：整圈计时显示、启动后计时起点、停止条件、停车点判定和异常状态提示，以满足题目对计时与显示的要求。

### 8.2 LED 最小链路指示

在 5 ms 中断中，LED 根据链路状态显示：未尝试发送、已发无字节、收到原始字节、解出合法帧及本地回环等不同状态。这使没有串口助手时也能初步判断 K230 是否供电、是否连线及是否能解帧。最终现场应在附录中说明每种闪烁节奏的含义，并录制一次从断线到恢复的演示。

### 8.3 UART0 调试遥测

UART0 每 50 ms 输出一行 `$T` 遥测，字段表头为：

```text
#TELEM_V3,t_ms,dt_ms,rx_ms,rx_age_ms,seq,x10,valid,edge,
v10,toward_v10,stop10,trim10,servo_us,out10,
ctrl,parse_err,crc_err,seq_gap,target_x10,task#
```

字段涵盖本地时刻、接收时刻/帧龄、视觉位置、有效标志、速度估计、停车距离、平衡角、舵机脉宽、控制状态、通信错误累计值、目标位置与任务状态。事件以 `$E,<t_ms>,<text>#` 单独输出，例如 `RESET`、`CAL,SAVED`、`TASK,PLUS`、`TASK,DONE`。

建议以同一会话编号保存 K230 与 MSPM0 日志，并在后处理时执行以下检查：

1. 比较两个文件的会话编号、文件大小、首尾时间和序号范围；
2. 检查 K230 发送序号与 MSPM0 `seq` 的连续性；
3. 统计 `rx_age_ms`、`dt_ms`、CRC 错误和解析错误；
4. 绘制 `x_mm`、目标位置、速度、舵机脉宽和 `ctrl` 状态随时间变化曲线；
5. 对照视频时间轴标出起球、过零、稳态、断链和边缘恢复时刻。

## 9. 控制参数与可追溯表

下表只列当前源码默认值，作为复现实验的参数快照，不代表最终最优参数。

| 类别 | 参数 | 当前值 | 源文件 |
| --- | --- | ---: | --- |
| 底盘 | 控制频率 | 200 Hz | `Control/control.h` |
| 底盘 | 轮距 | 0.1780 m | `Control/control.h` |
| 底盘 | 轮径 | 0.0768 m | `Control/control.h` |
| 底盘 | 编码器计数/圈 | 728 | `Control/control.h` |
| 底盘 | 速度 PI | Kp=400，Ki=300 | `Control/control.c` |
| 循线 | 基础速度 | 45 mm/s | `Control/control.h` |
| 循线 | 转向增益 | 1.70 | `Control/control.h` |
| 滚球 | PD | Kp=0.24 °/mm，Kd=0.040 °·s/mm | `Control/ball_control.h` |
| 滚球 | 目标容差 | 8 mm | `Control/ball_control.h` |
| 滚球 | 条件积分 | Ki=0.30 °/(mm·s)，限幅 ±6° | `Control/ball_control.h` |
| 滚球 | ACCEL | 24° 起步，每 300 ms 增 4°，最高 36° | `Control/ball_control.h` |
| 滚球 | 链路保持 | 总计 180 ms | `Control/ball_control.h` |
| 滚球 | 边缘恢复 | 相对 trim ±50°，最长 1000 ms | `Control/ball_control.h` |
| 舵机 | 默认平衡角 | 100.8° / 1660 μs | `Hardware/servo.h` |
| 视觉 | 发送周期 | 50 ms（配置值） | `k230/ball_detect_config.py` |
| 通信 | UART 波特率 | 115200 bit/s | `empty.syscfg` |

每次正式测试都应额外记录：Git 提交号、是否存在未提交修改、K230 脚本版本、K230 配置文件、舵机型号、供电电压、电池电量、测试场地、钢球规格、摆杆安装状态和环境光照。

## 10. 编译、下载与现场联调流程

### 10.1 MSPM0 编译

1. 用 Keil uVision5 打开 `project/mspm0/keil/empty_LP_MSPM0G3507_nortos_keil.uvprojx`。
2. 选择目标 `MSPM0G3507_Project`。
3. 编译后检查 `Objects/empty_LP_MSPM0G3507_nortos_keil.build_log.htm` 中的 `0 Error(s), 0 Warning(s)`，不要只依据 Keil 程序退出码判断。
4. 如改动过 SysConfig，只在 `project/mspm0/empty.syscfg` 内修改，再用 TI SysConfig 重新生成；比较生成文件内容后再纳入提交。
5. 编译产物 `Objects/`、`Listings/` 和个人 IDE 状态 `.uvoptx` 不应提交。

### 10.2 K230 部署

1. 在 K230 目标固件上确认 `import cv_lite` 可执行。
2. 上传 `k230_ball_detect_uart.py`、`ball_detect_config.py` 及其依赖。
3. 先使用预览脚本验证画面覆盖摆杆、球心方向、候选圆和有效标志。
4. 完成多点像素—毫米标定后才将 `CALIBRATION_READY` 置为真。
5. 接入 UART 前确认 IO40、IO41、GND、115200 bit/s 和共地；先检查 ACK、LED 和日志，再启用滚球闭环。

### 10.3 推荐联调次序

| 阶段 | 操作 | 通过判据 | 不通过时优先检查 |
| --- | --- | --- | --- |
| 1 | 仅舵机 | 低幅度命令方向、脉宽、机械限位正确 | PA8、供电、共地、花键、方向 |
| 2 | 仅 K230 预览 | 全行程球心连续、方向正确、无明显跳变 | ROI、光照、圆检测参数、相机固定 |
| 3 | 视觉标定 | 多点误差满足 ±1 cm 验收所需裕量 | 锚点、透视、摆杆刻度、相机位置 |
| 4 | UART 联调 | ACK、序号连续、无 CRC/溢出累计 | TX/RX 交叉、GND、波特率、中断优先级 |
| 5 | 静止调平 | 中心附近不持续单向漂移 | 平衡角、机构摩擦、坐标正方向 |
| 6 | 静止 ±50 mm | 完成往返，记录时间和最大误差 | Kp/Kd/积分/ACCEL/PASS 参数 |
| 7 | 底盘循线 | 低速稳定过直线、弯道和起停线 | 灰度极性、传感器位置、速度 PI |
| 8 | 低速行驶稳球 | A→B 与一圈日志/视频可复核 | 车体加速度、视觉延迟、舵机余量 |
| 9 | 赛题速度验证 | 满足各项时间、误差、停车指标 | 分段速度规划、机械刚度、参数重整定 |

## 11. 测试方案与结果记录模板

### 11.1 测试原则

- 每次试验建立唯一会话编号，视频、K230 日志、MSPM0 日志和参数表使用同一编号。
- 每个条件至少重复 3 次，报告均值、最大值、最小值和失败次数；不能只挑选最佳一次。
- 位置误差应以摆杆刻度的人工复核或经标定的视觉坐标为依据，并注明测量方法。
- 报告图表应保留原始单位、坐标轴、采样率、测试日期和软件版本。
- 对“未通过”的数据也应保留，并给出原因分析和下一步改进，而不是删除。

### 11.2 视觉标定记录表（待填写）

| 会话 | 设定实体位置/mm | 视觉均值/mm | 标准差/mm | 最大绝对误差/mm | 帧率/fps | valid 比例 | 结论 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 待填 | −100 |  |  |  |  |  |  |
| 待填 | −50 |  |  |  |  |  |  |
| 待填 | 0 |  |  |  |  |  |  |
| 待填 | +50 |  |  |  |  |  |  |
| 待填 | +100 |  |  |  |  |  |  |

### 11.3 静止定点测试表（赛题要求 3，待填写）

| 试次 | 会话 | 0→+50 mm 时间/s | +50 mm 最大误差/mm | +50 mm 稳定时间/ms | +50→−50 mm 时间/s | −50 mm 最大误差/mm | 总时间/s | 是否≤5 s 且误差≤10 mm |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 1 | 待填 |  |  |  |  |  |  |  |
| 2 | 待填 |  |  |  |  |  |  |  |
| 3 | 待填 |  |  |  |  |  |  |  |

应绘制至少一张完整的 `x(t)` 曲线，叠加 `+50 mm`、`−50 mm` 和 ±10 mm 容差带；第二张图叠加舵机 PWM、速度估计与控制状态，说明 ACCEL、PASS 和 CAPTURE 的切换是否合理。

### 11.4 循线与停车测试表（赛题要求 2，待填写）

| 试次 | 电池电压/V | 一圈时间/s | 停车偏差/cm | 脱线次数 | 灰度异常次数 | 是否通过 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 1 |  |  |  |  |  |  |
| 2 |  |  |  |  |  |  |
| 3 |  |  |  |  |  |  |

### 11.5 行驶稳球测试表（赛题要求 4～6，待填写）

| 场景 | 试次 | 行驶时间/s | 目标/mm | 全程最大误差/mm | 有效帧比例 | 最大帧龄/ms | CRC/序号间断 | 视频与日志路径 | 是否通过 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- | --- |
| A→B 中心稳球 | 1 |  | 0 |  |  |  |  |  |  |
| A→B 中心稳球 | 2 |  | 0 |  |  |  |  |  |  |
| A→B 中心稳球 | 3 |  | 0 |  |  |  |  |  |  |
| 一圈中心稳球 | 1 |  | 0 |  |  |  |  |  |  |
| 一圈中心稳球 | 2 |  | 0 |  |  |  |  |  |  |
| 一圈中心稳球 | 3 |  | 0 |  |  |  |  |  |  |
| 一圈任意点稳球 | 1 |  | 待填 |  |  |  |  |  |  |
| 一圈任意点稳球 | 2 |  | 待填 |  |  |  |  |  |  |
| 一圈任意点稳球 | 3 |  | 待填 |  |  |  |  |  |  |

### 11.6 通信可靠性与异常保护测试表（待填写）

| 场景 | 操作 | 预期软件行为 | 应采集证据 | 实测结论 |
| --- | --- | --- | --- | --- |
| K230 未发送 | 上电后不运行视觉脚本 | LED/诊断显示无合法帧，控制器不依据旧位置持续驱动 | OLED、UART0、LED 视频 | 待填 |
| 人为断开 TX | 运行中断开 K230→MSPM0 | 100 ms 超时后短暂 HOLD，总计 180 ms 后 LOST | `rx_age_ms`、`ctrl`、舵机脉宽 | 待填 |
| CRC 破坏 | 注入错误帧 | 计入 `crc_errors`，不更新控制位置 | 串口原始帧、遥测 | 待填 |
| 序号跳变 | 跳过若干帧 | 计入 `sequence_gaps`，系统保持安全运行 | 遥测 | 待填 |
| 球出视野 | 将球移向边缘并遮挡 | 若为边缘丢失则进入 EDGE，超时后停止恢复 | 视频、`ctrl`、PWM | 待填 |
| 标定中断 | 标定期间断开视觉或按键中止 | 退出标定且不写入无效数据 | 事件行、复位后 trim | 待填 |

## 12. 当前实现边界、风险与改进方向

### 12.1 已从源码确认的内容

- MSPM0 Keil 工程已包含滚球控制、自动标定、任务状态机、K230 链路、舵机和遥测源文件。
- SysConfig 已配置 UART1（K230）、UART0（遥测）、PA8 舵机 PWM、底盘电机 PWM、按键、OLED、灰度和编码器接口。
- K230 通信采用带 CRC、序号、有效标志和边缘方向的 ASCII 帧；主控存在超时和错误统计。
- 小球控制存在静摩擦起球、速度估计、停车距离制动、过零阻尼、条件积分、失联和边缘恢复分支。
- 标定采用非阻塞状态机，写 Flash 放在主循环而非 5 ms 中断。

### 12.2 不能由源码直接证明的内容

- K230 在目标 CanMV 固件上的 API、帧率、圆检测成功率和光照鲁棒性；
- 三点像素—毫米映射在全行程内的精度，及其是否满足 ±1 cm 目标的误差预算；
- 舵机实际角度与脉宽的映射、限位余量、响应速度、回差和供电抗扰；
- 摆杆机械平直度、槽面状态、钢球滚动阻力及当前平衡基准的长期重复性；
- 底盘在规定赛道的循线时间、停车偏差、弯道加速度和对滚球的扰动；
- 完整图传录像链路及赛题要求的回放证明；
- 当前在研未提交代码的编译结果与实车结果。

### 12.3 主要风险及对策

| 风险 | 可能后果 | 当前软件缓解措施 | 仍需的硬件/测试措施 |
| --- | --- | --- | --- |
| 视觉标定误差 | 误把不满足 ±1 cm 的位置当作达标 | 分段映射、有效标志、范围检查 | 多点重复标定、人工刻度复核 |
| 视觉低帧率或丢帧 | 速度估计噪声、延迟过大 | 仅新样本更新速度、滤波、HOLD/LOST | 测帧率与端到端延迟，优化 ROI/算法 |
| 静摩擦或局部凹陷 | 球停滞、ACCEL 过冲 | 分级起球、按误差缩放、PASS 阻尼 | 修复合规机械表面，重新整定参数 |
| 舵机饱和/回差 | 无法纠偏或振荡 | 角度/PWM 限幅，边缘恢复超时 | 标定实际极限与回差，检查供电 |
| 车体加速度扰动 | 行驶中球偏离中心 | 视觉闭环、制动与速度控制 | 降低加速度，按直线/弯道分别验证 |
| UART 接收抢占不足 | 隐蔽丢字节、错误控制 | UART1 高于 TIMER 中断优先级、错误计数 | 压力发送测试，必要时考虑 DMA |
| Flash 标定异常 | 下次上电使用错误平衡角 | Magic、范围校验、提交标志、回读 | 掉电与重复标定寿命测试 |

### 12.4 建议的后续软件完善项

1. 将“任意指定位置”的目标设置接入明确的人机交互或上位机命令，并规定目标范围、单位、确认和复位逻辑。
2. 将赛题的整车启动、计时、A 点停车识别和滚球故障联锁统一为高层状态机，避免底盘与滚球任务各自独立启动。
3. 为 K230 标定增加配置版本号、标定日期、锚点和误差统计输出，避免未知来源参数进入正式比赛。
4. 完成日志解析脚本：自动核对 K230/M0 会话、计算位置误差、帧龄、控制状态时长并输出报告图。
5. 在确认实际硬件资源后，评估 UART 接收 DMA、视觉帧时间戳同步和底盘加速度前馈的必要性。

## 13. 结论（最终报告撰写模板）

本设计采用 MSPM0G3507 + K230 的分层控制方案：MSPM0 负责红外循线、编码器速度闭环、舵机 PWM、通信安全和任务调度；K230 负责摆杆内钢球的视觉检测与位置输出。针对钢球控制中静摩擦、过零、视觉异步采样、通信超时和出视野等问题，软件实现了带 CRC 的 UART 协议、分阶段滚球控制、自动平衡角标定、Flash 持久化和时间对齐遥测。

在最终提交时，应将本段替换为基于测试表和原始日志的结论，明确写出：视觉最大标定误差、静止 ±50 mm 往返时间/最大误差、A→B 与整圈行驶时间/最大误差、停车偏差、成功率，以及异常保护测试结果。若某项仍未达标，应如实说明限制条件和改进方向，不应使用“理论上可达”“代码已实现”代替实测结果。

## 附录 A：报告插图与素材清单

最终排版时建议加入下列图件，并标明图号、图注、来源和测试条件：

1. 赛道路线图：[图 1](project/topic/pic/1_小车行驶路线示意图.png)；
2. 系统机械参考图：[图 2](project/topic/pic/2_车载平衡滚球运动控制系统结构参考示意图.png)；
3. 摆杆顶视与截面图：[图 3a](project/topic/pic/3a_摆杆结构示意图.png)、[图 3b](project/topic/pic/3b_摆杆截面放大图.png)；
4. 自绘系统框图、MSPM0 引脚图、K230 接线图、软件流程图和控制状态机图；
5. K230 预览截图（必须能看见球心、中心线/标定线和有效状态）；
6. 静止定点 `x-t`、`PWM-t`、控制状态曲线；
7. A→B 和整圈测试的球位置误差曲线、路线视频关键帧和统计柱状图；
8. UART0 遥测样例、K230 日志样例和视频文件索引表。

## 附录 B：源码可追溯索引

| 报告内容 | 主要源码/文档 |
| --- | --- |
| 赛题原始约束与评分 | `project/topic/车载平衡滚球运动控制系统_H题_markdown版_图片预览版.md` |
| 系统入口与中断优先级 | `project/mspm0/empty.c` |
| SysConfig 引脚和外设 | `project/mspm0/empty.syscfg` |
| 巡线、速度计算、PI | `project/mspm0/Control/control.c`、`control.h` |
| 小球控制状态机与参数 | `project/mspm0/Control/ball_control.c`、`ball_control.h` |
| 静止 ±50 mm 任务 | `project/mspm0/Control/ball_task.c`、`ball_task.h` |
| 自动标定 | `project/mspm0/Control/ball_calibrate.c`、`ball_calibrate.h` |
| K230 帧解析和诊断 | `project/mspm0/Hardware/k230_link.c`、`k230_link.h` |
| 舵机脉宽输出 | `project/mspm0/Hardware/servo.c`、`servo.h` |
| 标定 Flash 保存 | `project/mspm0/Hardware/calib_store.c`、`calib_store.h` |
| OLED 和遥测 | `project/mspm0/Control/show.c`、`debug_telemetry.c` |
| K230 检测和标定配置 | `project/k230/k230_ball_detect_uart.py`、`ball_detect_config.py` |
| K230 协议与日志说明 | `project/k230/ball_detect_uart.md` |

